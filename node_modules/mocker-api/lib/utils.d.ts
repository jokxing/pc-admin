import { TokensToRegexpOptions, ParseOptions } from 'path-to-regexp';
export declare function pathMatch(options: TokensToRegexpOptions & ParseOptions): (path: string) => (pathname: string, params?: any) => any;
