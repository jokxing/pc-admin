import { Request, Response, NextFunction } from 'express';
import { MockerOption, MockerProxyRoute } from '.';
declare type MockerHandleOptions = {
    req: Request;
    res: Response;
    next: NextFunction;
    options: MockerOption;
    mocker: MockerProxyRoute;
    mockerKey: string;
};
export declare function mockerHandle(param: MockerHandleOptions): void;
export {};
